# PostgreSQL를 내부 다른 포트로 포워딩하는 예시
upstream pg_backend { server 127.0.0.1:5432; }

server {
  listen 15432;                 # 들어오는 포트 (클라이언트는 여기에 접속)
  proxy_connect_timeout 3s;
  proxy_timeout 60s;
  proxy_pass pg_backend;
}
